[project]
name = "symptom-ai"
version = "0.1.0"
description = "AI-powered symptom simulation system"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv>=0.2.0"
]

[project.scripts]
start = "server.app:main"